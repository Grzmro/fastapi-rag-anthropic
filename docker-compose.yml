services:
  api:
    build: .
    ports:
      - "8000:8000"
    env_file: .env
    environment:
      # Keep the Chroma index on a host volume so it survives restarts.
      CHROMA_PERSIST_DIR: /app/chroma_data
    volumes:
      - ./chroma_data:/app/chroma_data
    restart: unless-stopped
